#include <iostream>
#include <vector>
using namespace std;

int main() {
    int a;
    cin >> a;
    vector<int> ans;
    for (int i = 2; i * i <= a; i++) {
        while (a % i == 0) {
            ans.push_back(i);
            a /= i;
        }
    }
    if (a != 1)
        ans.push_back(a);
    for (int i = 0; i < ans.size(); i++) {
        cout << ans[i] << ' ';
    }
}
