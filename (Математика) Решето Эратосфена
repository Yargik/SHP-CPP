#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<bool> ans(n + 1, false);
    for (int i = 2; i <= n; i++) {
        if (!ans[i])
            for (int j = i + i; j <= n; j += i)
                ans[j] = true;
    }
    for (int i = 2; i <= n; i++) {
        if (!ans[i])
            cout << i << ' ';
    }
}
