#include <iostream>
using namespace std;

int mydiv(int a, int b) {
    if (a >= 0)
        return a / b;
    else
        return (a - abs(b) + 1) / b;
}

int mymod(int a, int b) {
    int c = a - mydiv(a, b) * b;
    return c;
}

int main() {
    cout << -8 % 3 << '\n' << mymod(-8, 3) << '\n';
}
