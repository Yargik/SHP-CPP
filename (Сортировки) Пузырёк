#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

#define int long long

using namespace std;

signed main()
{
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++)
        cin >> arr[i];
    bool flag = true;
    for (int i = 0; i < n && flag; i++) {
        flag = false;
        for (int j = 0; j < n - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                flag = true;
                swap(arr[j], arr[j + 1]);
                //int tmp = arr[j];
                //arr[j] = arr[j + 1];
                //arr[j + 1] = arr[j];
                // Это и есть swap
            }
        }
    }
    for (int i = 0; i < n; i++) {
        cout << arr[i] << ' ';
    }
}
