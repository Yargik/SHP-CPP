#include <iostream>
#include <math.h>
#include <vector>
#define int long long
using namespace std;

signed main() {
    int a;
    vector<int> arr;
    while (cin >> a) {
        arr.push_back(a);
    }
}
